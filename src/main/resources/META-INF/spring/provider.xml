<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<dubbo:application name="WeatherDemo" />
	<!-- 注册中心服务地址 -->
	<dubbo:registry id="zookeeper" protocol="zookeeper"
		address="localhost:2181" />

	<!-- 用dubbo协议在20880 -->
	<dubbo:protocol name="dubbo" port="20880" />
	<!-- 声明需要暴露的服务接口 -->
	<dubbo:service
		interface="io.github.yexiaoxiaogo.weather.service.WeatherCityService"
		ref="WeatherCityService" />

	<!-- 具体服务接口的实现 -->
	<bean id="WeatherCityService"
		class="io.github.yexiaoxiaogo.weather.service.impl.WeatherCityServiceImpl" />

</beans>